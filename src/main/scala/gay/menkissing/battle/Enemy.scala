package gay.menkissing
package battle

import gay.menkissing.engine.GaySprite
import gay.menkissing.engine.GayTexture

class Enemy(val fighter: Fighter, texture: draw.Texture) extends GaySprite(GayTexture(texture)) {
  
}
